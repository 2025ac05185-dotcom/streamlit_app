# Pinned deliberately. scikit-learn MUST match the version that produced the
# .joblib files or unpickling emits version warnings / fails outright.
# Verified to resolve with binary wheels on Python 3.11, 3.12 and 3.13
# (Streamlit Community Cloud does not offer 3.14).
streamlit==1.61.1
scikit-learn==1.8.0
pandas==3.0.2
numpy==2.4.4
altair==6.2.2
joblib==1.5.3
tabulate==0.9.0
